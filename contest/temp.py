from typing import List


class Solution:
    def minBitwiseArray(self, nums: List[int]):
        ans = [0] * len(nums)
        for idx, val in enumerate(ans):
            val = nums                                
            val = nums                                
            val = nums                                
